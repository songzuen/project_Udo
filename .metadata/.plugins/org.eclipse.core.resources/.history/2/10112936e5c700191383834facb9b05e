package com.wad.udo.rv.controller;

import java.util.ArrayList;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;
import org.springframework.web.bind.annotation.RestController;

import com.wad.udo.rv.domain.CalDate;
import com.wad.udo.rv.domain.RvInfo;
import com.wad.udo.rv.service.DateService;

@RestController("date/getWeek")
public class DateController {

	@Autowired
	private DateService service;
	
	@CrossOrigin
	@GetMapping
	public ResponseEntity<List<CalDate>> getWeek() {
		
		List<CalDate> weekList = service.getWeek();
		ResponseEntity<List<CalDate>> entity = new ResponseEntity<List<CalDate>>(weekList, HttpStatus.OK);
		
		return entity;
	}
}
